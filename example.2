
module,rng

pool,state,uint32_t
pool,bogus,uint32_t,17
bundle,state,state,bogus
pool,simple,float,1


fn,stateless,x
lit,48271UL,A
lit,2147483647UL,B
mul,Axx 
mod,xBx
inc,x
end

fn,r,a type,uint32_t,B
state,B mov,BA stateless,A mov,Aa mov,AB
state-store,B
end

fn,f,a type,float,a
r,A mov,Aa
end

module,star

pool,x,float,65536
pool,y,float
band,star,x,y
pack,star

fn,create,i type,float,A
f@rng,A x-storei,Ai f@rng,A y-storei,Ai
c printf("CR %llu\n", *i);
end

fn,populate
each,x,NI create,I
end

fn,dump
lit,0,A
x-size,B
times,BI dumpone,I
bye
end


fn,dumpone,i type,float,X type,float,Y
x-i,Xi y-i,Yi
c printf("%lli %0.2f %0.2f\n", *i, *X, *Y);
end

fn,bye
c exit(0);


pro #include <stdio.h>
pro #include <stdlib.h>
anx int main() { udp_setup(); populate__from__star(); dump__from__star(); for(;;) udp_handler(); }

